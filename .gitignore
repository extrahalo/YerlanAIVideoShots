# macOS Finder noise
.DS_Store
**/.DS_Store

# Secrets — never commit API keys
.openai_key
.env
*.key

# Local generation scratch (test renders, workspace-specific mappings)
_gen_review/

# Note: unreviewed generated images are intentionally NOT ignored here,
# so they stay visible in `git status` for manual review before publishing.
assets/character-sheets-final/
assets/chars-confirmed/
